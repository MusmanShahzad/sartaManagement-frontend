mutation LoginUser($email:String,$password:String){
  LoginUser(email:$email,password:$password){
    Errors{
      error
      message
    }
    Data{
      user{
        _id
        name
        email
        userType
        createdDate
        status
      }
      token
    }
  }
}